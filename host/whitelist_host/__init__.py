# Minecraft-host half of the whitelist gateway.
#
# The version of the normalization CONTRACT this code implements (§4). The
# Worker pins the same number; the matcher refuses to run when they differ,
# and only matches submission rows created under this version.
NORMALIZATION_VERSION = 1
